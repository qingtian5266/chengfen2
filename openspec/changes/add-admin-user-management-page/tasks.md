## 1. 页面与路由骨架

- [x] 1.1 在 `packages/admin/src/router/index.ts` 新增用户管理路由（如 `/users`）并配置懒加载页面组件。
- [x] 1.2 新建 `packages/admin/src/views/UserManagementView.vue` 页面骨架，包含用户列表区与操作区。
- [x] 1.3 在 `packages/admin/src/App.vue` 增加到用户管理页的导航入口，确保可访问。

## 2. API 与类型封装

- [x] 2.1 在 admin 端新增 users API 模块，封装 `POST /users/list`、`/users/create`、`/users/update`、`/users/delete`。
- [x] 2.2 定义用户列表项和创建/更新表单参数的 TypeScript 类型。
- [x] 2.3 在 API 层统一处理请求异常与后端业务错误消息。
- [x] 2.4 固定列表请求策略（`page=1` + 约定 `size`），并在代码中集中管理该常量。

## 3. 列表与共享弹框 CRUD

- [x] 3.1 实现页面初次加载用户列表并渲染简单表格（无分页控件）。
- [x] 3.2 实现“新增用户”操作，打开共享弹框并在创建模式下提交 `/users/create`。
- [x] 3.3 实现“编辑用户”操作，复用同一弹框并在编辑模式下提交 `/users/update`。
- [x] 3.4 为共享弹框实现必填校验，校验失败时阻止提交并展示错误信息。
- [x] 3.5 实现“删除用户”确认流程，确认后调用 `/users/delete`，成功后刷新列表。
- [x] 3.6 确认列表行操作仅包含编辑和删除，不包含详情操作。

## 4. 验证与收尾

- [x] 4.1 手工验证主流程：列表加载、新增、编辑、删除可用且新增编辑确实复用同一弹框。
- [x] 4.2 手工验证异常流程：列表请求失败、创建/编辑业务报错时提示清晰且表单数据保留。
- [x] 4.3 清理调试日志与无用代码，确保改动通过项目既有 lint/构建检查。
