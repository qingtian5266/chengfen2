## 1. 首页入口修改

- [ ] 1.1 修改 `pages/index/index.vue` 的 `goRecognition` 方法，调用 `Taro.chooseImage`
- [ ] 1.2 配置 `chooseImage` 参数：`count: 1`, `sourceType: ['camera', 'album']`
- [ ] 1.3 处理用户成功选择图片的逻辑，获取 `tempFilePaths[0]`
- [ ] 1.4 处理用户取消选择的情况，不显示错误提示
- [ ] 1.5 处理选择失败的情况，显示轻提示并记录日志
- [ ] 1.6 选择成功后跳转至 `/pages/recognition/index`，传递图片路径参数

## 2. 识别页接收图片

- [x] 2.1 修改 `pages/recognition/index.vue`，在 `onLoad` 或 `setup` 中读取路由参数
- [x] 2.2 支持接收 `imageSource` 参数和图片临时路径
- [x] 2.3 若接收到图片参数，自动设置 `imageUrl` 并展示预览
- [x] 2.4 若无图片参数，保持现有"选择图片"按钮交互

## 3. OCR 识别与结果展示

- [x] 3.1 实现图片转 base64 方法（`filePathToBase64`）
- [x] 3.2 实现 OCR 识别中状态（加载动画或"识别中"提示）
- [x] 3.3 实现 OCR 识别成功后的结果展示 UI：
  - [x] 风险评级横幅（如"暂未发现风险成分"）
  - [x] 敏感成分区域（如有）
  - [x] 成分表格（成分名称、风险等级、成分标签、作用）
- [x] 3.4 实现风险等级图标和文案映射（A-无风险、B-低风险、C-中风险、D-高风险）
- [x] 3.5 实现 OCR 识别失败处理（错误提示 + 重新识别按钮）
- [x] 3.6 添加分享按钮功能（调用小程序分享能力）
- [x] 3.7 更新 API 接口定义，支持 base64 传参

## 4. 测试验证

- [ ] 4.1 测试从首页点击"拍照查成分"弹出选择器
- [ ] 4.2 测试选择相机拍照后正确跳转并展示图片
- [ ] 4.3 测试从相册选择图片后正确跳转并展示图片
- [ ] 4.4 测试用户取消选择后停留在首页
- [ ] 4.5 测试识别页能正确展示传入的图片
- [ ] 4.6 测试识别页独立访问时（无参数）功能正常
- [x] 4.7 测试 OCR 识别成功、失败、加载中的 UI 状态（构建通过）
